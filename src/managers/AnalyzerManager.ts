import { IAccount } from "../models/Account";
import { IMarketData } from "../models/MarketData";

export default class AnalyzerManager {

    /**
     * @param {Analyzer} analyzer
     */
    constructor() {
        
    } 


    // public async preferredCoinsHandler( account: IAccount, marketData: IMarketData) {

    //     const userPreferredCoins = account.preferred_coins;
    //     const marketDataCoins = marketData.coins;
        
        
        // const preferredCoinsHandler = new PreferredCoinsHandler();

        // for (let i = 0; i < preferredCoins.length; i++) {
        //     const coin = preferredCoins[i];
        //     const handler = preferredCoinsHandler.getHandler(coin);
        //     await handler.handle(marketData, coin);
        // }


    // }


}